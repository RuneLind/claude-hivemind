{
  "name": "claude-hivemind",
  "version": "0.2.0",
  "description": "Peer discovery and messaging for Claude Code instances with namespace isolation",
  "module": "src/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun src/cli.ts kill-broker 2>/dev/null; bun run --watch src/broker.ts",
    "start": "bun src/cli.ts kill-broker 2>/dev/null; bun run src/broker.ts",
    "server": "bun src/server.ts",
    "cli": "bun src/cli.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
